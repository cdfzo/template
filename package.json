{
  "name": "@cdfzo/template",
  "version": "0.1.0",
  "scripts": {
    "dev": "bun run src/index.ts",
    "lint": "echo 'TODO' && exit 1",
    "test": "bun test",
    "build": "echo 'TODO' && exit 1"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "latest"
  }
}
